<header class="header">
      <div class="header_bar">
        <h1 class="grid-6"><a href="/"><div class="minesanpo">Minesanpo</div></a></h1>
        <% if user_signed_in? %>
        <div class="username"><%= "Hi#{current_user.nickname}" %></div>
          <div class="user_nav grid-6">
            <span>
              <ul class="user__info">
                <li>
                  <%= link_to "Mine", "/users/#{current_user.id}" %>
                  <%= link_to "zzz...", destroy_user_session_path, method: :delete %>
                </li>
              </ul>
              <div class="newpost">
              <%= link_to "Post", new_post_path %>
              </div>
            </span>
              
          </div>
        <% else %>
          <div class="grid-6">
            <%= link_to "Login", new_user_session_path, class: "post" %>
            <%= link_to "Newuser", new_user_registration_path, class: "post" %>
          </div>
        <% end %>
      </div>
    </header>